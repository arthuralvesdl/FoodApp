import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    headerStyle: {
        backgroundColor: '#efefef', // Defina a cor de fundo desejada para o cabeçalho
        elevation: 0,
        tintColor: '#400101',
    },
    headerTitleStyle: {
        color: '#400101', // Defina a cor do texto do título do cabeçalho
        fontSize: 20, // Defina o tamanho da fonte do título do cabeçalho
        fontWeight: 'bold', // Defina o peso da fonte do título do cabeçalho
        textAlign: 'center'
    },
});

export default styles;